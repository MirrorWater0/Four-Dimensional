# 变更提案：示例功能 (example-feature)

## 背景
为演示 OpenSpec 工作流，创建一个示例变更提案。

## 目标
- 展示 OpenSpec 目录结构
- 验证 VS Code 扩展的按钮和上下文注入是否正常

## 范围
- 仅涉及文档和示例文件
- 不影响游戏运行时

## 验收标准
- [x] `openspec/` 目录结构创建完成
- [x] `AGENTS.md`、`project.md` 就位
- [x] 示例 `tasks.md` 可在 VS Code 中显示 ▶️ Start task 按钮
